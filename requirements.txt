fastapi
uvicorn[standard]
websockets
websocket-client
numpy<2.0
# ultralytics di-install terpisah dengan --no-deps di Dockerfile
# untuk menghindari konflik opencv dengan base image NVIDIA
pydantic
# Jangan install opencv-python, gunakan yang dari base image NVIDIA
# opencv-python-headless